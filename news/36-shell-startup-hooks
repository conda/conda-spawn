### Enhancements

* Inject Fish and Xonsh activation through their documented startup options so activation completes before the first interactive prompt. (#36)

### Bug fixes

* Preserve native login startup files and logout behavior for POSIX shells, Bash, and Zsh while spawning an activated shell. (#36)
* Run Xonsh `on_post_rc` handlers before activation and readiness. (#36)
* Remove temporary activation scripts as soon as the spawned shell reports it is ready. (#36)
* Keep conda-spawn's nested-session marker out of conda's `CONDA_*` environment-variable namespace. (#36)

### Deprecations

### Docs

### Other
